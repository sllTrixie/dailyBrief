# GEMINI.md - 全局开发与行为规范

> 本文档为项目顶层全局开发守则。AI 编程助手在执行任何开发、重构或调试任务时，必须严格遵守。

---

## 🧭 核心哲学与第一性原理

1. **简洁至上 (KISS)**：恪守 "Keep It Simple, Stupid" 原则。崇尚简单直接、高内聚且易维护的代码，坚决杜绝过度设计与无意义的防御性套壳。
2. **零占位符铁律 (Full Output)**：视每一次交付为生产级成品。绝对严禁在代码或文档中使用 `// TODO`、`// ...`、`/* 后续逻辑同上 */` 等敷衍占位符。
3. **事实为本**：代码与架构必须立足于当前工程实际代码与契约，严禁凭空臆断。

---

## 🪜 每次动手开发前的强制三步走 (代码复用阶梯)

在着手编写任何具体业务或功能实现前，必须严格按照以下四个优先级阶梯寻求解决方案，**严禁盲目从零造轮子**：

```
第一阶梯：GitHub 成熟开源方案 (必须是经过生产检验、高星且活跃维护的成熟代码)
   │
   ▼ (若无完全对应的成熟库)
第二阶梯：GitHub 相似项目参考 (借鉴相似业务场景、优秀架构范式或健壮接缝设计)
   │
   ▼ (若外部社区缺乏合适范式)
第三阶梯：个人/团队历史代码资产 (深入复用以往编写过的成熟模块、工具函数与适配器)
   │
   ▼ (前三步检索均无成熟方案)
第四阶梯：极简自研兜底 (遵循深模块设计，小接口、高内聚、高单测覆盖)
```

---

## 🌿 虚拟环境按需决策准则 (拒绝过度工程)

用户关切：“需不需要用虚拟环境啊？不需要的话就没必要了。”

**原则：按需启用，零冗余。技术栈天然自带隔离的，绝不增加虚拟环境负担。**

- **Node.js / TypeScript 项目（如当前 dailyBrief 工程）**：
  - **不需要虚拟环境 ❌**。
  - Node.js 天然将所有依赖隔离在当前项目的 `./node_modules` 目录下，本身就是天然的项目级物理隔离。额外配置 Python venv 或容器纯属画蛇添足。
- **Python 脚本与辅助工具**：
  - **必须强制使用本地虚拟环境 ✅**。
  - Python 默认全局安装会污染系统并破坏环境依赖。若项目中涉及 Python 核心开发，必须在本地创建 `.venv`（`python3 -m venv .venv`），激活后使用。
- **Go / Rust / 编译型语言**：
  - **不需要虚拟环境 ❌**，直接使用内置的 `go.mod` 或 `Cargo.lock`。

---

## 📦 依赖管理与 GitHub 仓库同步铁律

当因业务需要引入新依赖包时：

1. **依赖清单与锁文件必须同步推送到 GitHub**：
   - Node.js：必须同时提交 `package.json` 与 `package-lock.json`（必须保证 `npm ci` 在远端 Actions 中 100% 成功）。
   - Python：必须同步提交 `requirements.txt` 或 `pyproject.toml`。
2. **严禁将环境实体提交到 GitHub**：
   - 依赖与运行环境文件（`node_modules/`、`.venv/`、`__pycache__/`）**必须严格列入 `.gitignore`**，严禁推送到远程仓库。

---

## 🛡️ Git 提交前必须人工确认 & GitHub 自动化防护

### 1. 提交前强制确认
- **AI 严禁私自 `git commit` 或 `git push`**。
- 准备提交代码前，必须主动向用户展示：
  - **变更目的**：解决了什么问题？
  - **变更文件清单**：新增、修改、删除了哪些文件？
  - **拟使用的 Commit Message**。
  - **GitHub 自动化（CI/CD）影响自检结果**。
  - 必须等待用户明确回复确认后方可执行提交。

### 2. GitHub Actions 自动化兼容性自检清单
当前项目依赖 `.github/workflows/daily.yml` 定时拉取数据、调用 LLM 生成报告并自动发布到 GitHub Pages。任何提交必须防范以下问题：
- **`npm ci` 崩溃防御**：如果修改了依赖，是否严格同步了 `package-lock.json`？
- **运行环境一致性**：若新代码需要额外的环境或 CLI 工具，GitHub Actions 的 Ubuntu runner 是否支持？若不支持，需提前评估并在工作流中增加安装步骤。
- **定时调度防破坏**：不要无故篡改 `daily.yml` 的时区计算、时间栅栏 (`gate` job) 和 Pages 推送逻辑。
- **密钥防泄露**：敏感 Token/Key 必须走环境变量与 GitHub Secrets，绝不能硬编码提交。

---

## 🧹 仓库清洁（断舍离原则）

- **保留必要内容**：核心源代码、配置文件、依赖清单及锁文件、CI 流程文件、清晰文档。
- **删除冗余无害内容**：一次性调试脚本、临时的本地测试输出文件、过时无效的注释代码。

---

## 📚 详细子规则目录

更多维度的专项规则请参阅 `.agents/rules/` 目录：
- [开发工作流与代码复用规范](file:///.agents/rules/development-workflow.md)
- [环境与依赖管理规范](file:///.agents/rules/environment-and-dependencies.md)
- [Git 提交确认与 CI/CD 自动化保护规范](file:///.agents/rules/git-and-ci-safety.md)
